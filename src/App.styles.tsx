import { makeStyles } from "@mui/styles";

interface Props {
  froniusWidth: string;
}

export const useStyles = makeStyles({
  app: {
    height: "100%",
    display: "flex",
    flexDirection: "row",
  },
});
